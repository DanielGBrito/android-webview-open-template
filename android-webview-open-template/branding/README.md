# Branding Assets

Place the app logo in this directory.

Default expected file:

```text
logo.jpeg
```

The logo path is configured in the project `.env` file:

```properties
APP_LOGO_PATH=branding/logo.jpeg
```

Supported formats: `.jpeg`, `.jpg`, `.png`.
